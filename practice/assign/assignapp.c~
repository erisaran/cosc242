#include <stdio.h>
#include <stdlib.h>
#include "htable.h"
#include "mylib.h"

int main(void){
    htable h = htable_new(3877);
    htable_insert(h, "hi");
    htable_insert(h, "hi");
    htable_print(h, stdout);
    htable_free(h);

    return EXIT_SUCCESS;

}
